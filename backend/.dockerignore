# Ключи и настройки в образ не попадают: образ хранится и пересобирается,
# а секреты должны лежать только на сервере и подменяться без пересборки.
# Ключ Google Play прокидывается в контейнер томом (см. docker-compose).
play-service-account.json
*.p8
.env

data/
