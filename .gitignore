# Секреты — никогда не коммитить
.env
*.jks
key.properties

# Данные сервера (только backend; site/src/data — контент, он в git)
backend/data/

# Зависимости и сборки
node_modules/
build/
dist/
.astro/

# Flutter/Dart
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/android/.gradle/
app/android/local.properties

# Go
backend/bin/

# APK и version.json кладёт scripts/publish-apk.sh — артефакты, в репо не храним
site/public/app/

# Редакторы и ОС
.idea/
*.iml
.vscode/
*.swp
.DS_Store

# Ключи магазинов: сервисный аккаунт Google Play и ключ App Store Server API.
# Репозиторий публичный — попадание сюда означает чужой доступ к заказам.
backend/play-service-account.json
backend/*.p8
