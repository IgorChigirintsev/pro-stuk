---
import HowItWorks from '../../components/pages/HowItWorks.astro';
import { PREFIXED } from '../../i18n';

export function getStaticPaths() {
  return PREFIXED.map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params;
---

<HowItWorks lang={lang} />
